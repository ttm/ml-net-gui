const t = require('tone.js')
const s = require('scribbletune')
// use rythm.js to dance using a wav file,
// check html values in note values to set synth and visualization

module.exports = { t, s }
